kubectl create secret generic gcs-credentials --from-file=key.json=/home/juho/private-key.json
gsutil mb -p <my-gcp-project-id> gs://devops-kubernetes-backups

kubens project

export SOPS_AGE_KEY_FILE=/home/juho/key.txt
sops --decrypt postgres-secret.enc.yaml > postgres-secret.yaml 

kubectl apply -k .

kubectl get pods
kubectl logs -f 